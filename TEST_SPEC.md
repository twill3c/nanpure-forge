# TEST_SPEC.md — nanpure-forge

<!-- scaffold template v1.8.0 から展開(2026-08-17) -->

## 実行規約

- `cargo test --manifest-path rust/Cargo.toml` を stage 3–5 の判定に使用(本作は vitest でなく Rust 標準テスト)
- 完了条件は `python scripts/verify.py` green(fmt / clippy -D warnings / test / wasm ビルド)
- 制約充足の検算・mulberry32 参照値はテスト内に独立に書く(実装の流用禁止)
- 生成予算(T-1xx)は較正実験の証拠つきでのみ確定する(VERIF-FALSE 予防)
- 縁(空盤・解なし盤・二解盤)は正常系として挙動を固定する

## ケース一覧

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-001 | F-01/G-05 | mulberry32 の先頭 8 値 | JS 参照実装の値(テスト内にハードコード)と完全一致・[0,1) |
| T-010 | F-02/G-01 | 既知パズルの求解 | 既知の解と完全一致・解いた盤が全制約を満たす(テスト内で独立検算) |
| T-011 | F-02 | 空盤の求解 | 予算内に完解し全制約を満たす(候補順固定なので決定論) |
| T-012 | F-02 | 解なし盤 / 二解盤 | count_solutions が 0 / 2(上限打ち切り)を返し、solve は 0 / 1 |
| T-020 | F-03/G-02 | シード 1〜20 × 3 難易度 | 全 60 問で count_solutions == 1・所与マスは解と一致 |
| T-021 | F-03/G-03 | 同一シード+難易度 × 2 回 | 盤面まで完全一致 |
| T-022 | F-03/G-04 | ヒント数 | やさしい ≥38・ふつう ≤36・むずかしい ≤30(較正で確定)・難易度間で単調 |
| T-030 | F-04 | WASM 境界関数(ネイティブ側で検証) | buf 経由の generate → count_solutions_buf == 1 → solve_buf == 1 の一連が整合 |
| T-031 | F-04/F-06 | conflict_at | 同行に同数字 2 つ → 両方 1・無関係セルは 0・空セルは 0(独立ペア配置で検算) |
| T-032 | F-04/F-06 | is_complete_valid_buf | 完全盤 → 1・1 マス書き換え → 0・1 マス空き → 0 |
| T-040 | F-08 | フッタ 5 リンク(playwright で DOM 検査) | MIT License © 2026 坂田哲朗 の表記 + GitHub / 解き方 / 設計図 / App Menu の 4 アンカーが正しい href で存在 |
| T-100 | G-01/G-04/N-03 | 予算ゲート | 全 60 問の生成+求解の探索ノードが予算内(数値は較正済み) |
