# SPEC.md — nanpure-forge

<!-- scaffold template v1.8.0 から展開(2026-08-17)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

ナンプレ(9×9)の生成・求解・一意解検証を **Rust** で実装し、WebAssembly として
ブラウザで動かすパズルアプリ。UI は素の HTML / CSS / プレーン JavaScript グルー
(WASM ロードと DOM 反映のみ)で、**出荷物に TypeScript / Python を含まない**。
正しさの正本は `cargo test` の数理ゲート — 出題される全問題は一意解であることを
生成時に機械検証し、テストでも横断検証する。ビルド済み `.wasm` はコミットして
静的配信(Vercel は配るだけ)。

## 2. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | 乱数は mulberry32(フリート共通アルゴリズム)を Rust で実装しシード注入。生成は同一シード+難易度で決定論 | must |
| F-02 | ソルバー(Rust): ビットマスク+バックトラック。セル順・候補順固定で決定論。探索ノード予算付き。解の個数カウント(上限 2 で打ち切り)を持つ | must |
| F-03 | 生成器(Rust): 乱択バックトラックで完全盤を作り、ランダム順に空けながら**一意解を保つ**セルだけ削る。難易度 = 目標ヒント数(やさしい 40 / ふつう 32 / むずかしい 26・下限到達で終了) | must |
| F-04 | WASM 境界: 81 バイトの共有バッファ(0=空・1〜9=数字)+ 関数 `buf_ptr` / `generate(seed, difficulty) -> givens` / `solve_buf() -> 0/1` / `count_solutions_buf() -> 0/1/2` / `solution_at(idx)` / `conflict_at(idx) -> 0/1`(行・列・箱の重複判定)/ `is_complete_valid_buf() -> 0/1`(完成判定) | must |
| F-05 | UI: 9×9 盤(タップでセル選択 → 数字パッドで入力/消去)。ヒントマス(所与)は編集不可・見た目区別 | must |
| F-06 | 検査支援: 行・列・箱の重複をリアルタイムで赤ハイライト。全マス正しく埋まったら完成演出とタイム表示 | must |
| F-07 | 操作: 新しい問題(難易度 3 択・シード表示)・答え合わせ(誤りマス点滅)・ヒント(正解を 1 マス充填)・全消し | must |
| F-08 | フッタ: MIT License・GitHub・操作説明(解き方)・設計図・App Menu の 5 リンク | must |

## 3. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | 静的配信のみ: `index.html` + `style.css` + `app.js`(プレーン JS グルー)+ `nanpure.wasm`(コミット済みビルド成果物)。外部通信なし | Vercel 静的配信 |
| N-02 | アプリのロジックは全て Rust(`rust/src/lib.rs`)。JS グルーはロード・入出力・DOM 反映のみで、ナンプレの規則を一切知らない | コードレビュー基準(AGENTS §4) |
| N-03 | 生成・求解は探索予算付き(無限探索なし)。むずかしい生成でも 1 秒級 | cargo test の予算ゲート |
| N-04 | ポインタ / タッチのみで操作完結(キーボード不要。物理キーボードの数字入力は補助として許可) | 手動確認 |
| N-05 | 品質ゲートは `python scripts/verify.py`: cargo fmt --check / clippy(-D warnings)/ cargo test / wasm リリースビルド+web/ へ配置。verify green を全ループの完了条件とする | verify.py |

## 4. 品質基準

数理ゲート(cargo test。予算は較正実験の証拠つきで確定):

| ID | ゲート | 基準 |
|---|---|---|
| G-01 | ソルバー正当性 | 解いた盤が全制約(行・列・箱で 1〜9 一意)を満たす。既知の困難例(空盤・最少 17 ヒント級)も予算内に完解 |
| G-02 | 一意解の保証 | シード 1〜20 × 3 難易度の全 60 問で `count_solutions == 1`(生成器の主張をテストが独立再確認) |
| G-03 | 決定論 | 同一シード+難易度の 2 回の生成が盤面まで完全一致 |
| G-04 | ヒント数と予算 | 各難易度の生成ヒント数が目標 ±許容内・生成と求解が探索ノード予算内 |
| G-05 | mulberry32 の言語間一致 | Rust 実装の先頭 8 値が JS 参照実装(フリート共通)の値と完全一致(クロス言語照合) |

## 5. スコープ外

- 鉛筆メモ(候補メモ)・アンドゥ・タイマーランキング(将来ループ候補)
- 対称形の美観制約つき生成・難易度の技法採点(裸のシングル等)
- サーバ保存・対戦・キーボード専用 UI
